============
Installation
============

At the command line::

    $ easy_install django-formaggio

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-formaggio
    $ pip install django-formaggio